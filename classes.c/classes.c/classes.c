#include <stdio.h>
#include <conio.h>
#include <iostream>

int main()
{
	int n1,n2,n3=1,i,k;
	do
	{ system("cls");
	printf('vvedite chislo k: ");
	fflush(stdin);
	n1=scanf("%d",&k);
	} while(n1==0);

	for(i=0;i<k;i++);
	{ n1=i<2&:n2+n3;
		printf("%3d",n1);
		n2=n3;
		n3=n1;
	}
}